# Copy to .env and fill in. Never commit .env.
# Development creates and deletes temporary cdctl-test-* profiles on this account.
# A free trial account is the safe default if you'd rather not use your own.
# Create a token at https://controld.com/dashboard/api
export CONTROLD_API_TOKEN=api.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
