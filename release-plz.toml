[workspace]
git_release_enable = false  # cargo-dist owns the GitHub Release (D14)
release_always = false      # publish only when the release PR is merged, never on ordinary pushes
features_always_increment_minor = true  # a feat: commit is a minor release even pre-1.0 (roadmap.md)
